# Makefile - фаил в корне проекта, раскладывает по полочкам всем команды, плюс утилита make запускает из из того мейкфайла

# цель 
install: 
			npi ci
# команда 1 
brain-games: 
			node bin/brain-games.js